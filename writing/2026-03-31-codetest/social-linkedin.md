
I used AI to solve six coding exercises in six languages. 968 tests. Here's what that actually looks like in practice.

Cash register in rust, integer-cent arithmetic, zero floating-point bugs. The model wrote the trait implementations. I chose rust because money shouldn't have floating-point rounding errors, and rust's type system enforces that at compile time.

Missing number in zig, the model wrote the allocator-aware algorithm. I chose zig because it's a systems language worth demonstrating fluency in, and comptime features show well on small, performance-sensitive code.

Morse code in perl, 232 tests. The model wrote the regex engine and hash lookups. I chose perl because it's a text processing problem and perl is still the right tool for that domain.

On-screen keyboard in python, gilded rose in go, restaurant reviews in typescript/react with a full-stack dashboard integrating all five exercises as live panels. Same pattern every time: I made the design decisions, the model executed.

This is what using AI well looks like. 25 years of knowing what good code looks like, then directing an AI to produce it at speed.

968 tests. Six languages. One human making every architectural call.

https://georgelarson.me/writing/2026-03-31-codetest/

#AI #SoftwareEngineering #Rust #Zig #Go #Python #TypeScript
